import React, { Component, useState } from 'react'
// 如果自定义函数的函数名是以useXXX的形式写的,就认为是自定义hook
// 如果不是以useXXX形式写的,就认为是普通函数
function useFn() {
  const [count, setCount] = useState(0)




  
}

export default class App extends Component {
  render() {
    return <div></div>
  }
}
